import React from 'react';
import ReactDOM from 'react-dom';
import App from '../src/App.tsx';  // Certifique-se de que este caminho está correto

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
